name: Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

env:
  LANG: "de_DE.UTF-8"

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.24'

    - name: de_DE.UTF-8
      run: |
          sudo locale-gen de_DE.UTF-8
          sudo update-locale LANG=de_DE.UTF-8
    - name: date
      run: |
          date

    - run: |
        sudo apt-get -qq update && sudo apt-get install -yqq libreoffice libreoffice-l10n-de lloconv
    - run: libreoffice --headless --version

    - run: lloconv foo.ods foo.csv
    - run: lloconv foo.ods foo.html
    - run: lloconv foo.ods foo.fods

    - run: cat foo.csv
    - run: cat foo.html
    - run: cat foo.html
